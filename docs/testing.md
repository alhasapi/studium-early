# Testing

On Linux/macOS, run from the repository root:

```sh
./tests/run.sh
```

This runs unit and integration tests for arithmetic, English, and logic, followed by Lua syntax checks. Windows developers can run the individual `tests\run.sh` scripts under a Lua/Luanti test environment.
